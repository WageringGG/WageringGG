@page "/client/wagers"
@attribute [Microsoft.AspNetCore.Authorization.Authorize]
<h1 class="text-center">Client Wagers</h1>
@code { 
    protected override async Task OnInitializedAsync()
    {
        await base.OnInitializedAsync();
    }
}
